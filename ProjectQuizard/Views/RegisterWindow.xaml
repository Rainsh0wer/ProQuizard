<Window x:Class="ProjectQuizard.Views.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ProjectQuizard.ViewModels"
        mc:Ignorable="d"
        Title="Đăng ký - Quiz App" Height="500" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    
    <Window.DataContext>
        <vm:RegisterViewModel/>
    </Window.DataContext>
    
    <Grid Background="LightGray">
        <Border Background="White" CornerRadius="10" Margin="30" Padding="30">
            <StackPanel>
                <Label Content="ĐĂNG KÝ TÀI KHOẢN" Style="{StaticResource TitleLabelStyle}" 
                       FontSize="20" Margin="0,0,0,20"/>
                
                <Label Content="Tên đăng nhập: *" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource TextBoxStyle}"/>
                
                <Label Content="Email: *" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource TextBoxStyle}"/>
                
                <Label Content="Họ tên:" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource TextBoxStyle}"/>
                
                <Label Content="Mật khẩu: *" Style="{StaticResource LabelStyle}"/>
                <PasswordBox x:Name="PasswordBox" Style="{StaticResource PasswordBoxStyle}"/>
                
                <Label Content="Vai trò:" Style="{StaticResource LabelStyle}"/>
                <ComboBox ItemsSource="{Binding Roles}" 
                          SelectedItem="{Binding SelectedRole}"
                          Style="{StaticResource ComboBoxStyle}"/>
                
                <Label Content="{Binding ErrorMessage}" Foreground="Red" 
                       Style="{StaticResource LabelStyle}" FontSize="10"/>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                    <Button Content="Đăng ký" 
                            Style="{StaticResource PrimaryButtonStyle}"
                            Command="{Binding RegisterCommand}"
                            IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"
                            Width="80"/>
                    
                    <Button Content="Hủy" 
                            Style="{StaticResource ButtonStyle}"
                            Command="{Binding CancelCommand}"
                            Width="80"/>
                </StackPanel>
                
                <ProgressBar IsIndeterminate="{Binding IsLoading}" 
                             Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                             Height="5" Margin="5"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>